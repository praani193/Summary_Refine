# Summary_Refine
need to build a small agent workflow engine, something like a very simplified version of LangGraph. The idea is to create a system where we can define a sequence of steps (nodes), connect them, maintain a shared state, and run the workflow end-to-end via APIs. This is not meant to be complicated. 
